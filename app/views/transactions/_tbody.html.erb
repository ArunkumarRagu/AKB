<tr>
  <td><%= transaction.name %></td>
  <td><%= transaction.account.name %></td>
  <td><%= transaction.amount %></td>
  <td><%= transaction.prev_bala %></td>
  <td><%= transaction.balance %></td>
  <td><%= transaction.transaction_type.description %></td>
  <td><%= transaction.notes %></td>
  <td>
    <%= link_to 'Edit', edit_transaction_path(transaction), class: 'btn btn-outline-primary'  %>
    <%= link_to 'Delete', transaction, method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-outline-danger' %>
  </td>
</tr>