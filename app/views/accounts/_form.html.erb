<%= form_with(model: account, local: true) do |form| %>
  <% if account.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(account.errors.count, "error") %> prohibited this account from being saved:</h2>
      <ul>
        <% account.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field mb-3">
    <%= form.label :name, class: 'form-label' %>
    <%= form.text_field :name, class: 'form-control' %>
  </div>
  <div class="field mb-3">
    <%= form.label :description, class: 'form-label' %>
    <%= form.text_area :description, class: 'form-control' %>
  </div>
  <div class="field mb-3">
    <%= form.label :branch, class: 'form-label' %>
    <%= form.text_field :branch, class: 'form-control' %>
  </div>
  <div class="field mb-3">
    <%= form.label :balance, class: 'form-label' %>
    <%= form.number_field :balance, class: 'form-control' %>
  </div>
  <div class="actions mb-2 d-flex justify-content-center">
    <%= form.submit action, class: "btn btn-primary submit-btn"%>
    <%= link_to 'Back', path, class: 'btn btn-secondary' %>
  </div>
<% end %>
